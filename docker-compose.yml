name: lfcloud
services:
  webapp:
    image: node:24-alpine
    container_name: lfcloud
    volumes:
      - ./:/app
    working_dir: /app
    network_mode: host
    command: sh -c "npm install && npm run dev"
    env_file:
      - .env
