export default defineAppConfig({
  ui: {
    colors: {
      primary: "violet",
      secondary: "teal",
      neutral: "zinc",
    },
    inputNumber: {
      defaultVariants: {
        size: "xl",
      },
    },
  },
});
